import qrcode
text="hello students are u ok with studies"
a=qrcode.QRCode()
a.add_data(text)
a.make(fit=True)
res=a.make_image(fill_color="red",black_color="white")
res.save("cse.png")
print("the qr code is generated")
